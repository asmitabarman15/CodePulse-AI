import React from "react";

const CodeEditor = ({ value, onChange }) => {

  return (

    <textarea
      value={value}
      onChange={(e) => onChange(e.target.value)}
      className="w-full h-full bg-black text-green-400 p-4 font-mono"
    />

  );

};

export default CodeEditor;
